# 26.07.28 오전

## Git 사용법

## 콘솔 입력
cntl+alt+l : 방향키로 파일 찾을 수 있음, 솔루션 탐색기에서
중단점은 메모리나, 호출볼 때 사용
메모리 스냅샷을 보고, 검증하는 과정

무한루프 자주 사용이 될 것임.
작성할 때 무한루프를 깰 조건을 먼저 생각해야한다.

포맷 알아두기, int는 최소 4바이트

atoi?

단골
컴파일 빌드과정 설명해보세요.
C++ : 동작 방식 참고


1. 전처리
2. 컴파일
3. 어셈블
4. 링크

4단계를 거쳐 바이너리 만들어줌

1
- `#include`로 지정한 헤더의 내용 포함
- 매크로 확장
- 조건부 컴파일 처리
- 주석을 공백으로 대체

문법은 컴파일에서

2
- C++ 문법 검사
- 이름과 타입 검사
- 소스 코드를 대상 시스템의 명령으로 변환
- 어셈블러가 처리할 코드 생성

c++ 이 살아남은 이유?
최종실행할 때 속도가 빠르다는 이유 하나
include를 줄이는 게 속도가 빨라짐
최대한 줄이기위해 전방선언한다.
순환 참조하면 안됌.
실제로 파일 열고 닫음

극단적으로는 유니티 빌드가 있다. 자체 엔진 만드는 곳에서는 사용함. -> 빌드시간 줄일 수 있음
상용에서는 사용 못함



캐시 미싱
CPU 옆에있는 작업대 캐시
있을때 히트, 없을때 미싱
없으면 찾는데 디스크까지감 SSD


운영체제에 가상메모리라는게 있음, 운영체제와 메모리가 연결되어있음.
메모리 관리 할 때, 메모리주소를 가상화
cpp 로 프로그램 작성해서 메모리 줄래? 하면 메모리 공간 크기 요구하면
램에서 불러오겠지, 램이 16기가 들어오면 운영체제가 자름
64비트면 ㅇ다 자름, 메모리는 1차원으로 보고
1차원을 2차원처럼 쓰는거여

운영체제가 하는일은 자원관리는 하는거다. 메모리 키보드 마우스 다 자원
키보드 연결하면 드라이버 저절로 설치가 된다.
메모리가 8기가인데 부족하면 끌어다 씀

다 끌어다 쓰면 프로그램이든 운영체제든 뻗어

메모리를 ssd 까지 쓸 수 있는데 비어있는 공간 일부 씀
가상메모리쓰면 얻는 장점
가상메모리 말고 차선책 없나?
뭐 장점 단점?
가상 메모리 기법

스택, 힙 결국 메모리 사용하는 것

데이터영역, 코드영역
코드영역에 올라가야 실행이 된다.
데이터영역은 전역변수르ㅏㄹ 넣는다. 

스택, 힙 비교해라.
설명해라. 빌드과정 설명해라.


cpp 는 인라인 대체제가 있다고 함.
제너레이티드 바디에 이어짐 #define


인라인을 왜 써?
스택은 메모리를 쓰는데 인라인을 하면 코드 덩치가 더 커진다.
단순한코드 많지않음
인라인화 하고싶은 코드가. 그래서 코드의 양이 늘어남.
스택 메모리를 쓴다는건, 메모리 할당 해제 할당 해제 한다는거
그래서 속도가 느려질수도있다는거임
인라인은 스택메모리 쓸 수 없어서 속도를 얻고 메모리를 더 쓴다. 코드 용량도 늘어남.

인라인 7줄 이내, 뭐 규칙이 여러개 있다고함. 컴파일러한테 맡기는거임

특정 컴파일러에서는 __forceinline를 사용가능, 강력하게 인라인 쓰겠다고 하는거

define 알아두기



# typedef / using

using IntPtr = int*; 권장, 요즘은 using

using FunctionType = int (*)(char, double);
함수인데 포인터라는걸 알려주는거
typedef int (*FunctionType)(char, double);

함수포인터, 미리 보는거 추천

델리게이트?

함수의 주소를 저장하는게 함수포인터
주소 저장하는 기법이 포인터

포인터는 변수처럼 다룰 수 있다. 다르곳에 보낼 수 있음
어떤 객체에서 변수로 저장할 수 있다.

포폴에서, 이거 썼는데 왜 썼냐? 굳이? 가스? 이유와 의도 가지고 가야함.

함수는 선언을 하고 호출을 한다.

클릭 신호는 운영체제가 관리함.
하드웨어랑 연관된 신호는 운영체제

폴링 (끌어다가 쓰는거)

델리게이트의 핵심은 함수포인터이다.

## 템플릿
템플릿화 시킴

언리얼엔진 소스코드를 봤을 때 해석이 가능하게 정리


# auto
컴파일 시점에 타입 지정해줌
타입 추론이라, 추론할 수 있게 초기화 해야함.
근데 가급적 비추

명명 규칙도 있음

이터레이터 쓸 땐 auto 많이 사용한다.
복잡한 타입있으면 해석 맡기는 경우도 있다.

# 범위기반 For
이터레이터

auto 설명에 대해 나오긴함.



# 포인터
int가 뭘 정해? 타입 정함, 타입은 뭘 정해? 크기정함 메모리공간의 크기를, 0~255, 2의 7승까지, 4바이트 32비트
보통 세로 크기로 기준 많이 잡음.

int는 크기와 종류를 정함.
컴파일러에 따라 다를 수 있으나 ms 컴파일러 기준으로 4바이트
종류는 정수를 사용할 수 있게



## 2차원 배열 : 이미지


요즘은 설계를 덜 하는 방향으로 가고있다
왜? 공식화 된 경우가 많다. 단순한 구조가 좋다. 협업하기가 편하기 때문

포인터 주소 저장하는 곳
형 변환은 성공 및 실패 가능
